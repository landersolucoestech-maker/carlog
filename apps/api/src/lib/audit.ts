import type { PoolClient } from 'pg';
export async function recordAudit(client:PoolClient,input:{actorUserId?:string|null;action:string;entityType:string;entityId:string;before?:unknown;after?:unknown;correlationId?:string|null;metadata?:Record<string,unknown>}):Promise<void>{await client.query(`insert into app.audit_events(actor_user_id,action,entity_type,entity_id,before_data,after_data,correlation_id,metadata) values($1,$2,$3,$4,$5::jsonb,$6::jsonb,$7,$8::jsonb)`,[input.actorUserId??null,input.action,input.entityType,input.entityId,JSON.stringify(input.before??null),JSON.stringify(input.after??null),input.correlationId??null,JSON.stringify(input.metadata??{})])}
