import type { PoolClient } from 'pg';
export async function enqueueDomainEvent(client:PoolClient,input:{id?:string;type:string;actorUserId?:string|null;correlationId:string;causationId?:string|null;payload:Record<string,unknown>;source:string;idempotencyKey:string;occurredAt?:string}):Promise<string>{const id=input.id??crypto.randomUUID();await client.query(`insert into app.domain_events(id,type,actor_user_id,correlation_id,causation_id,payload_version,payload,source,idempotency_key,occurred_at) values($1,$2,$3,$4,$5,1,$6::jsonb,$7,$8,$9) on conflict(idempotency_key) do nothing`,[id,input.type,input.actorUserId??null,input.correlationId,input.causationId??null,JSON.stringify(input.payload),input.source,input.idempotencyKey,input.occurredAt??new Date().toISOString()]);return id}
