import type { PoolClient } from 'pg';import type { IntegrationCapability,IntegrationProviderKey } from '@carlog/integrations';
export async function enqueueIntegrationAction(client:PoolClient,input:{provider:IntegrationProviderKey;capability:IntegrationCapability;operation:string;payload:Record<string,unknown>;idempotencyKey:string;correlationId:string;availableAt?:string}):Promise<string>{const result=await client.query<{id:string}>(`insert into app.integration_actions(provider,capability,operation,payload,idempotency_key,correlation_id,status,available_at) values($1,$2,$3,$4::jsonb,$5,$6,'pending',coalesce($7::timestamptz,now())) on conflict(idempotency_key) do update set idempotency_key=excluded.idempotency_key returning id`,[input.provider,input.capability,input.operation,JSON.stringify(input.payload),input.idempotencyKey,input.correlationId,input.availableAt??null]);return result.rows[0]!.id}
